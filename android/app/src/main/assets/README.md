Optional runtime assets live here.

Place `All_nand_300.bin` in this directory before building if you want the
debug APK to boot the default NAND image without using Android's file picker.
The app also supports choosing a NAND image from device storage at runtime.
