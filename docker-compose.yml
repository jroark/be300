services:
  mips-dev:
    build: .
    image: mips-cross-dev
    volumes:
      - .:/work
    working_dir: /work
    environment:
      - PKG_CONFIG_PATH=/work/third_party/unicorn-linux/lib/pkgconfig
    command: /bin/bash
    stdin_open: true
    tty: true
